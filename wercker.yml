box:
  id: openjdk:8-jdk
  ports:
    - 8080

dev:
  steps:
    - script:
        name: info/env
        code: env
    - internal/watch:
        name: Run and Watch
        code: |
          ./gradlew bootRun
        reload: true 
build:
  steps:
    - script:
        name: info/env
        code: env

use-my-step:
  steps:
    - shtfresh/hello-world@1.0.0
use-helm-step:
  steps:
    - vamsiramakrishnan/helm@1.1.4
    - script:
         name: show message
         code:  |
           cat $HOME/.kube/config